{
  "name": "paint-pwa-extension",
  "private": true,
  "version": "1.0.0",
  "license": "GPL-3.0-only",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "check": "tsc --noEmit",
    "lint": "eslint .",
    "format": "prettier --write .",
    "test": "vitest run",
    "zip-source-code": "mkdir -p dist && zip -r dist/paint-pwa-source-v1.0.0.zip . -x \"node_modules/*\" \"dist/*\" \".git/*\" \"backup/*\" \".github/*\" \"scratch/*\" \".env.development\" \".env.production\"",
    "build:chrome": "TARGET=chrome vite build --mode development && TARGET=chrome BUILD_TARGET=content vite build --mode development && cd dist/chrome && zip -r ../paint-pwa-chrome-v1.0.0.zip .",
    "build:firefox": "TARGET=firefox vite build --mode development && TARGET=firefox BUILD_TARGET=content vite build --mode development && cd dist/firefox && zip -r ../paint-pwa-firefox-v1.0.0.zip .",
    "build:safari": "TARGET=safari vite build --mode development && TARGET=safari BUILD_TARGET=content vite build --mode development && cd dist/safari && zip -r ../paint-pwa-safari-v1.0.0.zip .",
    "build": "tsc && pnpm run build:chrome && pnpm run build:firefox && pnpm run build:safari && pnpm run zip-source-code",
    "build:chrome:release": "TARGET=chrome vite build --mode production && TARGET=chrome BUILD_TARGET=content vite build --mode production && cd dist/chrome && zip -r ../paint-pwa-chrome-v1.0.0.zip .",
    "build:firefox:release": "TARGET=firefox vite build --mode production && TARGET=firefox BUILD_TARGET=content vite build --mode production && cd dist/firefox && zip -r ../paint-pwa-firefox-v1.0.0.zip .",
    "build:safari:release": "TARGET=safari vite build --mode production && TARGET=safari BUILD_TARGET=content vite build --mode production && cd dist/safari && zip -r ../paint-pwa-safari-v1.0.0.zip .",
    "build:release": "tsc && pnpm run build:chrome:release && pnpm run build:firefox:release && pnpm run build:safari:release && pnpm run zip-source-code"
  },
  "dependencies": {
    "clsx": "^2.1.1",
    "fp-ts": "^2.16.0",
    "io-ts": "^2.2.21",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-tea-cup": "5.1.3",
    "tailwind-merge": "^3.3.1",
    "tea-cup-fp": "1.1.1"
  },
  "devDependencies": {
    "@eslint/js": "^9.29.0",
    "@tailwindcss/vite": "^4.1.18",
    "@trivago/prettier-plugin-sort-imports": "^6.0.2",
    "@types/chrome": "^0.0.299",
    "@types/react": "^19.1.8",
    "@types/react-dom": "^19.1.6",
    "@vitejs/plugin-react": "^6.0.2",
    "eslint": "^9.29.0",
    "eslint-plugin-import": "^2.32.0",
    "eslint-plugin-unused-imports": "^4.4.1",
    "globals": "^16.2.0",
    "prettier": "^3.8.1",
    "tailwindcss": "^4.1.18",
    "typescript": "~5.8.3",
    "typescript-eslint": "^8.34.1",
    "vite": "^8.0.14",
    "vitest": "^4.1.9"
  }
}
